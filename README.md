# Цифровой двойник космического аппарата (КА)

Состоит из:
* "двойника" КА
* конфигуратора системы управления движением (СУД)
* "виртуального баллистика"

## "Двойник" КА

Это консольное приложение scTwin, которое представляет систему управления базой конфигураций КА. Как минимум при запуске "двойника" в консоли должен быть 1 параметр - номер конфигурации КА. Все конфигурации записаны в файле twBase.json.
Основные функции:
1. Выбор нужной конфигурации из базы (twBase.json)
2. Копировать файл с описанием системы управления движением КА и его свойств в папку сonfigInOut. Этот файл относительно scTwin находится в папке ./scConfig/out42 и имеет имя SC_<имя_аппарата>.txt.
3. Копировать файл орбиты в папку сonfigInOut. Относительно scTwin он находится в orbConfig и имеет имя Orb_<имя_аппарата>.txt.
4. Изменять файл ../42support/CMakeLists.txt в зависимости от версии БПО. Версии БПО находятся в папке swConfig.
5. Собирать программу моделирования функционирования КА "Проект 42"
6. Запускать собранный "Проект 42", при этом папка исходных данных (ИД) назначается configInOut.
7. В папке configInOut уже лежит файл с ИД сценария моделирования (по-умолчанию) Inp_Sim.txt и пустой файл команд Inp_Cmd.txt.

По результатам запуска "Проект 42" должен сформировать значения основных показателей работы КА по заданному сценарию. При этом scTwin на основе этих значений должен сформировать обобщённый показатель и занести его в базу конфигураций.

Сборка scTwin стандартными средствами Qt, ЯП - ANSI C, файл проекта scTwin.pro

## Конфигуратор СУД

Основная цель этого консольного приложения (configs) сформировать из файла конфигурации СУД КА (acos/<имя_аппарата>.json) входной файл для "Проекта 42" (out42/SC_<имя_аппарата>.txt). 
При этом в файле конфигурации записаны только назавния типовых приборов и их кол-во, описание приборов находится в соответствующих файлах - acos/dev/<тип_прибора>.json
Основные функции:
1. о входному аргументу определить необходимый файл acos/<имя_аппарата>.json
2. На основе шаблона out42/SC_Template.txt создаём новый файл out42/SC_<имя_аппарата>.txt в соответствии с файлом acos/<имя_аппарата>.json

Сборка configs стандартными средствами Qt, ЯП - ANSI C, файл проекта scConfig/configs.pro

## "Виртуальный баллистик"

https://github.com/kharisovilyas/sdt_orbit_nlu